# Rendez-vous de suivi 2

**Date** : 02/06/2026  
**Heure** : 13h00 - 14h00  
**Participants** :

- Glück Florent
- Arcidiacono Jérémie

## Tâches réalisées

- Finalisation du diagramme Gantt prévisionnel
- Conception
    - archi global antsd + serf + k3s
    - mécanisme/logique de premier démarrage bootstrap
    - architecture logicielle antsd
    - recherche sur une manière de monitorer les nodes durant développement
- recherche communication k3s<->antds (github.com/kubernetes/client-go)
- recherche sur la création d'une image OS custom
    - rpi-image-gen
    - Hashicorp Packer

## Sujets à aborder

### antsd : exposition d'une API (monitoring, contrôle, etc) ?

Une API complète n'est pas utile, car aucun autre service ne l'utilisera

Donc on peut exposer seulement quelques points http basique :

- GET /status : état, role, liste des membres, etc...
- GET /debug/pprof/\* : profilage
- POST /removenode : donner l'ordre qu'une machine va être décommissionnée, pour qu'elle puisse quitter définitivement
  le cluster

métrique avec prometheus ?

### antsd : Serf sous forme de lib ou processus standalone ?

Oui, sous forme lib

### rpi-image-gen vs Hashicorp Packer

Plutôt Packer, surtout qu'au final ANTS n'utiliseront pas de Raspberry Pi

Avantages :

- flexibilité
- indépendance de la plateforme

### Full K3s Server, sans Agent

Voir [ici](../../architecture.md)

Investigué si un nombre pair de serveurs (etcd) pose réellement un problème en pratique.  
Si oui : un mécanisme qui s'assure que le nombre de serveurs est impair et qu'il ne dépasse pas 7 doit être mis en
place.  
Voir si c'est simple de convertir agent<->server dans les 2 sens

## Discussion

Appli client pour voir l'état du cluster "bas niveau"  
Pour trouver les autres node : serf aussi sur le client ?

Soit chaque node local possède cette logique et expose les point http.  
Soit un client externe, qui découvre avec serf les autres nodes.

Peut-être avoir un nom de domaine auto config (ex: ants.local) qui renvoi à une ip/toutes les ip des nodes

Comment en tant que user on a accès aux infos/controles de antsd sans connaitre la topologie du cluster ??

## Tâches à faire

- Investiguer le "client" qui permettra de voir et manipuler le cluster
- Investiguer la problématique K3s server/agent, migration, nombre pair/impaire
- Création d'une première image OS avec Hashicorp Packer
- Commencer à créer le planning effectif
